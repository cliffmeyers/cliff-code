version: 2
# version updates only, security updates enabled in GitHub Settings
# monthly + grouped to avoid PR floods
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    groups:
      actions:
        patterns: ["*"]
